{
  "name": "keeper",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm run build && node dist/test/keeper.test.js",
    "start": "node dist/runner.js",
    "start:monitor": "node dist/runner.js auction-monitor",
    "start:dispatcher": "node dist/runner.js cofhe-dispatcher",
    "start:avs": "node dist/runner.js avs-submitter"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ethers": "^6.14.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  }
}
