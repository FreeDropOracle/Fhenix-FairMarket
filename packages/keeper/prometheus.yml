global:
  scrape_interval: 15s

scrape_configs:
  - job_name: keeper-services
    static_configs:
      - targets:
          - auctionMonitor:9400
          - cofheDispatcher:9400
          - avsSubmitter:9400
